.. TrajTracker : Hotspot.py

Hotspot class
=============

This defines a touch-sensitive area on screen, that invokes certain operations when touched.

The area can be also configured to invoke the operation only if touched for a certain period of time.

The invoked operation can be a function to call, an :doc:`event <../events/events_overview>` to dispatch, or both.


Methods and properties:
-----------------------

.. autoclass:: trajtracker.movement.Hotspot
   :members:
   :inherited-members:
   :member-order: alphabetical

