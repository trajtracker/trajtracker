# Fields in the config CSV file

Target = "target"                       # The target number (must be numeric)
PresentedTarget = "presented_target"    #
OnsetTime = "onset_time"                # Onset time of each text (must have as many entries as presented_target)
Duration = "duration"                   # Duration to show each text
Size = "size"                           # Size - width;height (semicolon-separated)
